<%= content_for :page_title do %>Edit Profile: <%= @user.username %><% end %>

<div class="row">
  <div class="col-md-8">
    <div class="card card-primary card-outline">
    <%= render 'form', user: @user, resource: resource %>
    </div>
  </div>
</div>
