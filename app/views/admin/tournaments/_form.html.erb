<%= form_with(model: admin_tournament, url: admin_tournaments_path(admin_tournament), html: { class: "form-horizontal", role: "form"}) do |form| %>
  <% if admin_tournament.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_tournament.errors.count, "error") %> prohibited this admin_tournament from being saved:</h2>

      <ul>
        <% admin_tournament.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

                  <div class="form-group">
                    <%= form.label :name, "Name", class: "control-label col-sm-3" %>
                    <div class="col-sm-4">
                      <%= form.text_field :name, class: "form-control", placeholder: "Tournament name" %>
                    </div>
                  </div>
                  <div class="form-group">
                    <%= form.label :upload_players, "Upload players", class: "control-label col-sm-3" %>
                    <div class="col-sm-4">
                    <div class="input-group">
                      <div class="custom-file">
                        <%= file_field_tag :tournaments_players, class: "custom-file-input", accept: "text/csv" %>
                        <%= form.label :tournaments_players,"Choose file", class: "custom-file-label" %>
                      </div>
                      <div class="input-group-append">
                        <span class="input-group-text">Upload</span>
                      </div>
                    </div>
                    </div>
                  </div>

                <div class="actions">
                  <button type="submit" class="btn btn-primary">Submit</button>
                </div>

<% end %>
