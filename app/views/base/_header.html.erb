  <!-- Navbar -->
  <nav class="main-header navbar navbar-expand navbar-orange navbar-light">
    <!-- Left navbar links -->
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
      </li>
      <li class="nav-item d-none d-sm-inline-block">
        <%= link_to "Home", root_path, class: "nav-link" %>
      </li>
    </ul>

    <!-- Right navbar links -->
    <ul class="navbar-nav ml-auto">

      <li class="nav-item dropdown user-menu">
        <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown">
          <%= image_tag current_user.gravatar_url, class: "user-image img-circle elevation-2", alt: "User Image" %>
          <span class="d-none d-md-inline"><%= current_user.fullname %></span>
        </a>
        <ul class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
          <!-- User image -->
          <li class="user-header bg-primary">
            <%= image_tag current_user.gravatar_url, class: "img-circle elevation-2", alt: "User Image" %>
            <p>
              <%= current_user.fullname %>
              <div style="font-weight: bold;"><%= "Admin" %></div>
            </p>
          </li>
          <!-- Menu Footer-->
          <li class="user-footer">
            <%= link_to "Profile", admin_user_path(current_user), class: "btn btn-default btn-flat" %>
            <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-default btn-flat float-right" %>
          </li>
        </ul>
      </li>
      <li class="nav-item">
        <a class="nav-link" data-widget="control-sidebar" data-slide="true" href="#" role="button">
          <i class="fas fa-th-large"></i>
        </a>
      </li>
    </ul>
  </nav>
  <!-- /.navbar -->
